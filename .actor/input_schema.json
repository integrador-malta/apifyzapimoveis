
{
  "title": "Configuração da coleta",
  "description": "Forneça links de pesquisa do Zap Imóveis para coletar imóveis.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "links": {
      "title": "Links de pesquisa",
      "type": "array",
      "items": { "type": "string" },
      "editor": "stringList",
      "default": [],
      "description": "Lista de URLs de pesquisa do Zap Imóveis. Cole aqui os links completos com filtros já aplicados."
    },
    "usarProxy": {
      "title": "Usar proxy do Apify",
      "type": "boolean",
      "default": true,
      "description": "Habilita o proxy do Apify (recomendado para reduzir bloqueios)."
    },
    "maxPagesPorBairro": {
      "title": "Máximo de páginas por link",
      "type": "number",
      "editor": "number",
      "default": 10,
      "description": "Limite de paginação por link para controlar custo e tempo de execução."
    },
    "headless": {
      "title": "Browser headless",
      "type": "boolean",
      "default": true,
      "description": "Executa o navegador em modo headless (sem UI)."
    }
  },
  "required": ["links"]
}
